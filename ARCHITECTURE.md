# 海克斯助手 (Hextech Assistant)

> 英雄联盟海克斯大乱斗海克斯推荐工具

## 📖 产品概述

### 核心功能

| 功能模块 | 描述 | 优先级 |
|---------|------|--------|
| **英雄胜率榜** | 展示所有英雄的胜率排名，辅助选人决策 | P0 |
| **海克斯推荐** | 针对指定英雄，按品质（棱彩/黄金/白银）展示海克斯胜率排名 | P0 |
| **搜索功能** | 支持英雄名搜索 | P0 |
| **效果描述** | 海克斯效果说明（可展开） | P1 |
| **热门组合** | 高胜率海克斯组合推荐 | P2 |

### 用户场景

**场景一：选人阶段**
> "这把选什么英雄容易赢？"

- 查看英雄胜率总榜
- 按胜率/场次/选取率排序
- 点击英雄查看详情

**场景二：选海克斯阶段**
> "我是格温，面前三个棱彩海克斯，选哪个胜率最高？"

- 进入英雄详情页
- 选择当前品质 Tab
- 查看该品质内胜率排名
- TOP3 优先选择，后30%避免

---

## 🏗️ 技术架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Next.js 14)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 首页     │  │ 英雄详情  │  │ 海克斯排名│              │
│  │ (SSG)    │  │ (SSG)    │  │ (Client) │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────┐
│                    数据源 (JSON)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │champions │  │ augments │  │ rankings │              │
│  │.json    │  │ .json    │  │/*.json   │              │
│  └──────────┘  └──────────┘  └──────────┘              │
└─────────────────────────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **框架** | Next.js | 16.x | App Router, SSG |
| **语言** | TypeScript | 5.x | 类型安全 |
| **样式** | Tailwind CSS | 4.x | 原子化样式 |
| **UI组件** | shadcn/ui | latest | Radix + Tailwind |
| **图标** | Lucide React | latest | 统一图标 |
| **部署** | Vercel | - | 静态导出 |

### 项目结构

```
hextech-assistant/
├── app/                          # Next.js App Router
│   ├── page.tsx                  # 首页 - 英雄胜率榜
│   ├── layout.tsx                # 根布局 + PWA配置
│   ├── globals.css               # 全局样式
│   ├── champions/[id]/page.tsx   # 英雄详情页
│   └── components/               # 页面组件
│       ├── ChampionList.tsx      # 英雄列表
│       ├── SearchBox.tsx         # 搜索框
│       └── AugmentRanking.tsx    # 海克斯排名
├── components/ui/                # shadcn/ui 组件
├── lib/                          # 工具库
│   ├── types.ts                  # TypeScript 类型
│   └── data.ts                   # 数据操作
├── data/                         # 静态数据
│   ├── champions.json            # 英雄数据
│   ├── augments.json             # 海克斯数据
│   └── rankings/*.json           # 英雄-海克斯关联
└── public/
    └── manifest.json             # PWA配置
```

---

## 📊 数据模型

### Champion（英雄）

```typescript
interface Champion {
  id: number;           // 英雄ID
  name: string;         // 中文名
  title: string;        // 称号
  key: string;          // 英文名
  tier: 'T1'|'T2'|'T3'|'T4'|'T5';  // 梯度
  winRate: number;      // 胜率
  pickRate: number;     // 选取率
  games: number;        // 场次
  roles: string[];      // 定位标签
}
```

### Augment（海克斯）

```typescript
interface Augment {
  id: string;           // 唯一标识
  name: string;         // 中文名
  nameEn: string;       // 英文名
  tier: 'prismatic'|'gold'|'silver';  // 品质
  description?: string; // 效果描述
}
```

### ChampionAugmentData（英雄海克斯数据）

```typescript
interface ChampionAugmentData {
  championId: number;
  championName: string;
  version: string;      // 游戏版本
  updatedAt: string;    // 更新时间
  byTier: {
    prismatic: TierRanking;
    gold: TierRanking;
    silver: TierRanking;
  };
}

interface TierRanking {
  total: number;        // 该品质总数量
  medianWinRate: number; // 中位胜率
  augments: ChampionAugment[];
}

interface ChampionAugment {
  id: string;
  name: string;
  winRate: number;      // 该英雄携带此海克斯的胜率
  pickRate: number;     // 选取率
  games: number;        // 样本场次
  rank: number;         // 品质内排名
  score?: 'S'|'A'|'B'|'C'|'D';  // 评分
}
```

---

## 🔄 数据流

### 构建时 (SSG)

```
npm run build
    ↓
读取 data/champions.json
    ↓
生成首页静态 HTML（英雄列表）
    ↓
读取 data/rankings/*.json
    ↓
生成各英雄详情页静态 HTML
    ↓
输出到 dist/ 目录
```

### 运行时

```
用户访问
    ↓
加载静态页面（秒开）
    ↓
React Hydrate（可交互）
    ↓
Tab 切换等交互（客户端）
```

---

## 🚀 部署

### 构建命令

```bash
npm run build
```

输出目录：`dist/`

### Vercel 部署

1. 连接 GitHub 仓库
2. 框架预设：Next.js
3. 构建命令：`npm run build`
4. 输出目录：`dist`

### 静态导出配置

```typescript
// next.config.ts
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  images: { unoptimized: true },
  trailingSlash: true,
};
```

---

## 📱 PWA

### 功能

- ✅ 添加到主屏幕
- ✅ 离线访问（浏览器缓存）
- ✅ 深色模式支持

### 配置

```json
// public/manifest.json
{
  "name": "海克斯助手",
  "short_name": "HexAssistant",
  "display": "standalone",
  "theme_color": "#3b82f6",
  "background_color": "#0f172a"
}
```

---

## 🔮 未来扩展

### Phase 2

- [ ] 数据自动同步（GitHub Actions + 爬虫）
- [ ] 海克斯效果描述完整化
- [ ] 热门组合推荐

### Phase 3

- [ ] 快速决策功能（输入2-3个海克斯对比）
- [ ] 装备推荐
- [ ] 羁绊系统查询

### Phase 4

- [ ] 用户收藏英雄
- [ ] 离线数据更新
- [ ] 多语言支持

---

## 📝 维护指南

### 更新英雄数据

编辑 `data/champions.json`

### 更新海克斯数据

编辑 `data/augments.json`

### 更新英雄-海克斯关联

编辑 `data/rankings/{championId}.json`

### 添加新英雄

1. 在 `champions.json` 添加基础信息
2. 创建 `rankings/{id}.json` 海克斯数据
3. 重新构建部署

---

## 🔗 相关链接

- **数据来源**: https://hextech.dtodo.cn/ (ARAM.GG)
- **部署平台**: Vercel
- **仓库**: https://github.com/ww1126811502/hextech-assistant

---

*最后更新: 2026-02-08*
